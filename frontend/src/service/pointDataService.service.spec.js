describe('pointDataService', function() {

    beforeEach(module('timegraph'));

    it('should ...', inject(function(pointDataService) {

        //expect(pointDataService.doSomething()).toEqual('something');

    }));

});
