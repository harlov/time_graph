(function(angular) {
    'use strict';

    angular.module('timegraph').controller('AboutCtrl', AboutCtrl);

    function AboutCtrl() {
        var aboutCtrl = this;

        /* put bindable members here like
         aboutCtrl.buttonClick = buttonClick;
          */

        init();


        function init() {
            //do stuff to init controller
        }
    }
})(angular);
